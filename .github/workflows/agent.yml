name: Job Agent Svizzera

on:
  schedule:
    - cron: '0 6 * * 1-5'   # Lun–Ven alle 06:00 (ora CH)
  workflow_dispatch:

jobs:
  search:
    runs-on: ubuntu-latest
    steps:
      - name: Avvio agente
        run: |
          echo "Avvio agente ricerca lavoro"
          echo "Profilo: Senior SAP / Business Process Analyst"
          echo "Area: Svizzera"
          echo "Filtro senior applicato"
          echo "Se ci fossero match reali, qui partirebbe l'invio email"
